defmodule MinhaUniversidade.Repo.Migrations.MigrateResources12 do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:faculties) do
      add :logo_url, :text
    end
  end

  def down do
    alter table(:faculties) do
      remove :logo_url
    end
  end
end
